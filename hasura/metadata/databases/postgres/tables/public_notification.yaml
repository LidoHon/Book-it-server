table:
  name: notification
  schema: public
configuration:
  column_config:
    userId:
      custom_name: userId
  custom_column_names:
    userId: userId
  custom_root_fields: {}
object_relationships:
  - name: user
    using:
      foreign_key_constraint_on: userId
